import Foundation

// Owned by the RenderEngine packet — replace this file, keep the type/entry-point signature.
enum RenderCommand {
    static func run(arguments: [String]) async throws {
        throw CLIError.notImplemented("render")
    }
}
