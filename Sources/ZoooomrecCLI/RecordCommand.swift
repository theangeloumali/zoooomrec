import Foundation

// Owned by the CaptureEngine packet — replace this file, keep the type/entry-point signature.
enum RecordCommand {
    static func run(arguments: [String]) async throws {
        throw CLIError.notImplemented("record")
    }
}
