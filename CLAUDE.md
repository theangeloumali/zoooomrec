# zoooomrec

All contributor and agent instructions live in **[`AGENTS.md`](./AGENTS.md)** — architecture invariants, working agreements, build/test commands, and the traps we have already paid for. It is vendor-neutral and applies to every model and every human.

**Read [`AGENTS.md`](./AGENTS.md), then [`docs/BACKLOG.md`](./docs/BACKLOG.md), before writing any code.**

This file exists only so Claude Code auto-loads that pointer. Keep it a pointer — never duplicate the content here, or the two will drift.
